<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.db2phase3">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".registerParent"/>
        <activity android:name=".registerStudent"/>
        <activity android:name=".loginParent"/>
        <activity android:name=".loginStudent"/>

        <activity android:name=".studentDashboard"/>
        <activity android:name=".studentViewSections"/>



        <!-- Enter Mike's Activities Below Here-->
        <activity android:name=".parentDashboard"/>
        <activity android:name=".ChangeParentProfile"/>
        <activity android:name=".viewSectionsParent"/>
        <activity android:name=".ChangeChildProfile"/>
        <!-- Enter Mike's Activities Above Here -->

        <!-- Enter Ross' Activities Below Here-->
        <activity android:name=".menteeViewSessions"/>
        <activity android:name=".studentViewSessions"/>
        <activity android:name=".viewMentor"/>
        <activity android:name=".studentChangeProfile"/>
        <!-- Enter Ross' Activities Above Here -->

    </application>

</manifest>